<% layout('layouts/boilerplate') %>

    <h1>All Riffs</h1>
    <% if (currentUser) { %>
    <div>
        <a class="btn btn-success" href="/songs/new">Add</a>
    </div>
    <% } %>
    <% for (let song of songs) { %>
    <div class="card mb-3 text-center justify-content-center" >
        <div class="row justify-content-center" >
            <div class="col-md-8 justify-content-center">
                <h2 class="card-title"><%= song.title %></h2>
                <video width="auto" height="300" src="<%= song.image %>" controls preload="auto"></video>
            </div>
        </div>
        <div class="row-md-4 justify-content-center text-center">
            <!-- <p class="card-text"><%= song.artist %></p> -->
                <a class="btn btn-primary" href="/songs/<%= song._id %>">View <%= song.title %></a>
        </div>
    </div>
    <% } %>
   